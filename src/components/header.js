import react from 'react';
import styled from 'styled-components';
import {fontFamily, fontSize, colours} from '../theme';

const Header = styled.section`
    display: flex;
    justify-content: space-between;
`

export default Header = 